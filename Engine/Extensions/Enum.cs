using System;

namespace GU1.Engine.Extensions;

public static class EnumExtensions {

    // TODO: Figure out how to make this work
    //public static int GetLength(this Enum e) => Enum.GetValues(e.GetType()).Length;
}
